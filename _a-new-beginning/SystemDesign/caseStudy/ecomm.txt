browse, add to card, checkout
mobile friendly, responsive, offline caching, image cdn, i18n, security, seo friendlty

component:
product listing, product view, cart component, checkout flow

architecture:
ssr + hydration of csr

data model:
product,
product list [filter, search, pagination and sort param]
cart item,
user
address,
payment detail


note: price to be calculated on server due to promo code or bulk order usage.

api:
product list (offset based pagination)
product
cart op
place order: {address, card id, }
fetch cart detail
fetch address detail

optimisation:
code split, lazy load,
priorotise above the fold,
paginated response,
cdn bsed images, lazy load, webp, srcset responsive image, shimmer
virtualisation,
defer js scripts
prefetch responses 
seo meta tags, semantic html tags and proper use of role and aria-tags
optimise form filling based on locale, use autofilling tags for address and user detail,
cc-number, cc-name, cc-exp, address-level1,2,3, inputmode=numeric

i18n: <html lang="zh-cn"> hints browser to offer transalted page